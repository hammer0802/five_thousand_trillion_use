name: five_thousand_trillion_use
description: 5000兆円使い切りたい！！！！！！

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  hooks_riverpod: ^0.12.1
  state_notifier: ^0.6.0
  freezed: ^0.12.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
